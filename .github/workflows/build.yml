name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  cuda-compile:
    runs-on: ubuntu-24.04
    container:
      image: nvidia/cuda:13.0.2-devel-ubuntu24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build tools
        run: |
          apt-get update
          apt-get install -y --no-install-recommends build-essential cmake ninja-build python3
          rm -rf /var/lib/apt/lists/*

      - name: Toolchain
        run: |
          nvcc --version
          cmake --version
          g++ --version
          python3 --version

      - name: Python syntax validation
        run: |
          python3 -m compileall \
            experiments/10_pytorch_extension \
            experiments/11_triton \
            experiments/12_llm_kernels

      - name: Configure
        run: cmake --preset ci

      - name: Build
        run: cmake --build --preset ci-build

      - name: Enumerate GPU tests
        run: ctest --test-dir build/ci -N
